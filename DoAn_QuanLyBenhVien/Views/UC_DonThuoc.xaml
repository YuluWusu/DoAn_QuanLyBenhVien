<UserControl x:Class="DoAn_QuanLyBenhVien.Views.UC_DonThuoc"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DoAn_QuanLyBenhVien.Views"
             xmlns:vm="clr-namespace:DoAn_QuanLyBenhVien.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1100"
             Background="#0F1923">

    <UserControl.DataContext>
        <vm:DonThuocViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#2D4A6E"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="#00C2A8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#00C2A8"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#00C2A8"/>
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#F0F0F0"/>
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Control" x:Key="FadeDisabledStyle">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.4"/>
                    <Setter Property="Background" Value="Gray"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#2D4A6E"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#2D4A6E"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid x:Name="Root">
                            <Border x:Name="Underline" 
                                BorderThickness="0,0,0,3" 
                                BorderBrush="Transparent" 
                                Margin="5,0"
                                VerticalAlignment="Bottom"/>
                            <ContentPresenter x:Name="ContentSite"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        ContentSource="Header"
                                        Margin="12,10"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="True">
                                <Setter TargetName="ContentSite" Property="TextBlock.Foreground" Value="#00C2A8"/>
                            </Trigger>

                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Underline" Property="BorderBrush" Value="#00C2A8"/>
                                <Setter TargetName="ContentSite" Property="TextBlock.FontWeight" Value="Bold"/>
                                <Setter TargetName="ContentSite" Property="TextBlock.Foreground" Value="#00C2A8"/>
                            </Trigger>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ContentSite" Property="TextBlock.Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#162431"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#253D5C"/>
            <Setter Property="RowBackground" Value="#1A2E45"/>
            <Setter Property="AlternatingRowBackground" Value="#1E344E"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#253D5C"/>
            <Setter Property="Foreground" Value="#00C2A8"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#00C2A8"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <TabControl Background="Transparent" BorderThickness="0" SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <TabItem Header="ĐƠN THUỐC">
                <Grid Margin="0,15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Background="#162431" CornerRadius="8" Padding="15" Margin="0,0,0,15" BorderBrush="#253D5C" BorderThickness="1">
                        <WrapPanel>
                            <StackPanel Margin="0,0,20,10">
                                <TextBlock Text="Mã Đơn Thuốc" Foreground="#00C2A8" Margin="0,0,0,5" FontSize="11"/>
                                <TextBox Width="120" Height="32" Text="{Binding MaDonThuocMoi}" IsEnabled="{Binding IsProcessing}"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,20,10">
                                <TextBlock Text="Mã Phiếu Khám" Foreground="#00C2A8" Margin="0,0,0,5" FontSize="11"/>
                                <TextBox Width="120" Height="32" Text="{Binding MaPhieuKhamMoi}" IsEnabled="{Binding IsProcessing}"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,20,10">
                                <TextBlock Text="Ngày Kê" Foreground="#00C2A8" Margin="0,0,0,5" FontSize="11"/>
                                <DatePicker Width="150" Height="32" SelectedDate="{Binding NgayKeMoi}" IsEnabled="{Binding IsProcessing}"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,20,10">
                                <TextBlock Text="Mã NV Kê" Foreground="#00C2A8" Margin="0,0,0,5" FontSize="11"/>
                                <TextBox Width="100" Height="32" Text="{Binding MaNV_KeMoi}" IsEnabled="{Binding IsProcessing}"/>
                            </StackPanel>
                        </WrapPanel>
                    </Border>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15" HorizontalAlignment="Center">
                        <Button Content="{Binding AddButtonContent}" Command="{Binding AddCommand}" Width="90" Margin="0,0,10,0" Background="#00C2A8" Foreground="White"/>
                        <Button Content="📝 Sửa" Command="{Binding UpdateCommand}" IsEnabled="{Binding EditDeleteVisibility}" Width="90" Margin="0,0,10,0" Background="#FFB900" Foreground="White"/>
                        <Button Content="🗑 Xóa" Command="{Binding DeleteCommand}" IsEnabled="{Binding EditDeleteVisibility}" Width="90" Margin="0,0,10,0" Background="#FF4655" Foreground="White"/>
                        <Button Content="💾 Lưu" Command="{Binding SaveCommand}" IsEnabled="{Binding SaveButtonVisibility}" Width="90" Background="#2196F3" Foreground="White" Margin="0,0,10,0"/>
                        <Button Content="💊 Kê Thuốc" Command="{Binding ChiTietCommand}" IsEnabled="{Binding EditDeleteVisibility}" Width="110" Background="#8E2DE2" Foreground="White" FontWeight="Bold"/>
                    </StackPanel>
                    <DataGrid Grid.Row="2" ItemsSource="{Binding DanhSachDonThuoc}" SelectedItem="{Binding SelectedDonThuoc}" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã Đơn" Binding="{Binding MaDonThuoc}" Width="100"/>
                            <DataGridTextColumn Header="Mã Phiếu" Binding="{Binding MaPhieuKham}" Width="100"/>
                            <DataGridTextColumn Header="Ngày Kê" Binding="{Binding NgayKe, StringFormat='dd/MM/yyyy'}" Width="120"/>
                            <DataGridTextColumn Header="Mã NV" Binding="{Binding MaNV_Ke}" Width="100"/>
                            <DataGridTextColumn Header="Bác Sĩ Kê" Binding="{Binding TenBacSiKe}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="CHI TIẾT ĐƠN THUỐC">

                <Grid Margin="0,15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Background="#162431" CornerRadius="8" Padding="15" Margin="0,0,0,15" BorderBrush="#253D5C" BorderThickness="1">
                        <WrapPanel>
                            <StackPanel Margin="0,0,20,10">
                                <TextBlock Text="Mã Đơn Thuốc" Foreground="#00C2A8" Margin="0,0,0,5" FontSize="11"/>
                                <TextBox Width="100" Height="32" Text="{Binding SelectedDonThuoc.MaDonThuoc}" IsReadOnly="True"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,20,10">
                                <TextBlock Text="Chọn Thuốc" Foreground="#00C2A8" Margin="0,0,0,5" FontSize="11"/>
                                <ComboBox Width="200" Height="32" ItemsSource="{Binding DanhSachThuocGoc}" IsEnabled="{Binding IsProcessing}"
                                        SelectedItem="{Binding SelectedThuocHienTai}" DisplayMemberPath="TenThuoc"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,20,10">
                                <TextBlock Text="ĐVT" Foreground="#00C2A8" Margin="0,0,0,5" FontSize="11"/>
                                <TextBox Width="60" Height="32" Text="{Binding DVTMoi}" IsReadOnly="True"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,20,10">
                                <TextBlock Text="Số Lượng" Foreground="#00C2A8" Margin="0,0,0,5" FontSize="11"/>
                                <TextBox Width="70" Height="32" Text="{Binding SoLuongMoi, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsProcessing}"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,20,10">
                                <TextBlock Text="Giá Bán" Foreground="#00C2A8" Margin="0,0,0,5" FontSize="11"/>
                                <TextBox Width="90" Height="32" Text="{Binding GiaBanMoi, StringFormat='N0'}" IsReadOnly="True"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,20,10">
                                <TextBlock Text="Liều Dùng" Foreground="#00C2A8" Margin="0,0,0,5" FontSize="11"/>
                                <TextBox Width="220" Height="32" Text="{Binding LieuDungMoi, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsProcessing}"/>
                            </StackPanel>
                        </WrapPanel>
                    </Border>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15" HorizontalAlignment="Center">
                        <Button Content="➕ Thêm" Command="{Binding AddCommand}" Width="90" Margin="0,0,10,0" Background="#00C2A8" Foreground="White"/>
                        <Button Content="📝 Sửa" Command="{Binding UpdateCommand}" IsEnabled="{Binding EditDeleteVisibility}" Width="90" Margin="0,0,10,0" Background="#FFB900" Foreground="Black"/>
                        <Button Content="🗑 Xóa" Command="{Binding DeleteCommand}" IsEnabled="{Binding EditDeleteVisibility}" Width="90" Margin="0,0,10,0" Background="#FF4655" Foreground="White"/>
                        <Button Content="💾 Lưu" Command="{Binding SaveCommand}" IsEnabled="{Binding SaveButtonVisibility}" Width="90" Background="#2196F3" Foreground="White"/>
                    </StackPanel>

                    <DataGrid Grid.Row="2" ItemsSource="{Binding DanhSachChiTietDon_HienThi}" SelectedItem="{Binding SelectedChiTiet}" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã Thuốc" Binding="{Binding MaThuoc}" Width="100"/>
                            <DataGridTextColumn Header="Tên Thuốc" Binding="{Binding TenThuoc}" Width="2*"/>
                            <DataGridTextColumn Header="Số Lượng" Binding="{Binding SoLuong}" Width="80"/>
                            <DataGridTextColumn Header="ĐVT" Binding="{Binding DVT}" Width="70"/>
                            <DataGridTextColumn Header="Giá Bán" Binding="{Binding GiaBan, StringFormat='N0'}" Width="100"/>
                            <DataGridTextColumn Header="Liều Dùng" Binding="{Binding LieuDung}" Width="2.5*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
