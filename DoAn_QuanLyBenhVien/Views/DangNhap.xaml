<Window x:Class="DoAn_QuanLyBenhVien.Views.DangNhap"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Phòng Khám - Đăng Nhập"
        Height="480" Width="820"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#0F1923">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="LblStyle" TargetType="TextBlock">
            <Setter Property="Foreground"  Value="#6B8299"/>
            <Setter Property="FontFamily"  Value="Segoe UI"/>
            <Setter Property="FontSize"    Value="11"/>
            <Setter Property="FontWeight"  Value="SemiBold"/>
            <Setter Property="Margin"      Value="0,0,0,6"/>
        </Style>

        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Background"       Value="#162032"/>
            <Setter Property="Foreground"       Value="#E2EAF3"/>
            <Setter Property="FontFamily"       Value="Segoe UI"/>
            <Setter Property="FontSize"         Value="13"/>
            <Setter Property="BorderBrush"      Value="#1E3048"/>
            <Setter Property="BorderThickness"  Value="1"/>
            <Setter Property="Padding"          Value="12,10"/>
            <Setter Property="CaretBrush"       Value="#00C2A8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#00C2A8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PasswordStyle" TargetType="PasswordBox">
            <Setter Property="Background"       Value="#162032"/>
            <Setter Property="Foreground"       Value="#E2EAF3"/>
            <Setter Property="FontFamily"       Value="Segoe UI"/>
            <Setter Property="FontSize"         Value="13"/>
            <Setter Property="BorderBrush"      Value="#1E3048"/>
            <Setter Property="BorderThickness"  Value="1"/>
            <Setter Property="Padding"          Value="12,10"/>
            <Setter Property="CaretBrush"       Value="#00C2A8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#00C2A8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnLogin" TargetType="Button">
            <Setter Property="Background"      Value="#00C2A8"/>
            <Setter Property="Foreground"      Value="White"/>
            <Setter Property="FontFamily"      Value="Segoe UI"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="FontWeight"      Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="0,12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#0A8F7C"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#077363"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>

            <ColumnDefinition Width="340"/>

        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#0D1720">
            <StackPanel VerticalAlignment="Center"
                        HorizontalAlignment="Center">

                <Border Background="#00C2A8"
                        CornerRadius="20"
                        Width="64" Height="64"
                        HorizontalAlignment="Center"
                        Margin="0,0,0,20">
                    <TextBlock Text="🏥"
                               FontSize="32"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>

                <TextBlock Text="Đồ án"
                           Foreground="#E2EAF3"
                           FontFamily="Segoe UI"
                           FontSize="28"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Phần mềm Quản lý Phòng khám Tư nhân"
                           Foreground="#6B8299"
                           FontFamily="Segoe UI"
                           FontSize="12"
                           HorizontalAlignment="Center"
                           Margin="0,8,0,40"
                           TextAlignment="Center"/>

                <StackPanel Margin="40,0">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <Border Background="#0E2236" CornerRadius="6"
                                Width="28" Height="28" Margin="0,0,10,0">
                            <TextBlock Text="👤" FontSize="13"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Quản lý hồ sơ bệnh nhân"
                                   Foreground="#6B8299"
                                   FontFamily="Segoe UI" FontSize="12"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Border Background="#0E2236" CornerRadius="6"
                                Width="28" Height="28" Margin="0,0,10,0">
                            <TextBlock Text="💊" FontSize="13"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Quản lý thuốc &amp; kho dược"
                                   Foreground="#6B8299"
                                   FontFamily="Segoe UI" FontSize="12"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>

            </StackPanel>
        </Border>

        <Border Grid.Column="1"
                Background="#111D2B"
                BorderBrush="#1E3048"
                BorderThickness="1,0,0,0">
            <StackPanel VerticalAlignment="Center"
                        Margin="36,0">

                <TextBlock Text="Đăng nhập"
                           Foreground="#E2EAF3"
                           FontFamily="Segoe UI"
                           FontSize="22"
                           FontWeight="Bold"
                           Margin="0,0,0,4"/>

                <TextBlock Text="Vui lòng nhập thông tin tài khoản"
                           Foreground="#6B8299"
                           FontFamily="Segoe UI"
                           FontSize="12"
                           Margin="0,0,0,32"/>

                <TextBlock Text="TÊN ĐĂNG NHẬP"
                           Style="{StaticResource LblStyle}"/>
                <TextBox x:Name="TxtTenDangNhap"
                         Style="{StaticResource InputStyle}"
                         Text="{Binding TenDangNhap, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,0,16"/>

                <TextBlock Text="MẬT KHẨU"
                           Style="{StaticResource LblStyle}"/>
                <PasswordBox x:Name="TxtMatKhau"
                             Style="{StaticResource PasswordStyle}"
                             Margin="0,0,0,8"/>

                <TextBlock x:Name="TxtLoi"
                           Text="{Binding ThongBaoLoi}"
                           Foreground="#E05C5C"
                           FontFamily="Segoe UI"
                           FontSize="11"
                           Margin="0,0,0,20"
                           Visibility="{Binding CoLoi, Converter={StaticResource BoolToVis}}"/>

                <Button Content="ĐĂNG NHẬP" x:Name="BtnDangNhap"
                        Style="{StaticResource BtnLogin}" Click="BtnDangNhap_Click"
                        />

                <TextBlock Text="Phiên bản 1.0.0"
                           Foreground="#2A3F55"
                           FontFamily="Segoe UI"
                           FontSize="11"
                           HorizontalAlignment="Center"
                           Margin="0,24,0,0"/>

            </StackPanel>
        </Border>
    </Grid>
</Window>
