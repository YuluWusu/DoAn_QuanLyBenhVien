<UserControl x:Class="DoAn_QuanLyBenhVien.Views.UC_HoSo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DoAn_QuanLyBenhVien.Views"
             xmlns:vm="clr-namespace:DoAn_QuanLyBenhVien.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:HoSoKhamViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style x:Key="DarkTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#475569"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#10B981"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                                <Setter Property="Background" Value="#1E293B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="#1E293B"/>
            <Setter Property="AlternatingRowBackground" Value="#1E293B"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#334155"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#334155"/>
                        <Setter Property="Foreground" Value="#94A3B8"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#475569"/>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.3"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#7F1D1D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.3"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#B91C1C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SaveButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.3"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="WarningButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F59E0B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.3"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="#0F172A">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#1E293B" CornerRadius="12" Padding="20" Margin="0,0,0,20">
                <StackPanel>

                    <UniformGrid Columns="3">
                        <StackPanel Margin="0,0,15,0">
                            <TextBlock Text="Mã Phiếu Khám" Foreground="#94A3B8" FontSize="12" Margin="0,0,0,5"/>
                            <TextBox Style="{StaticResource DarkTextBox}" Text="{Binding MaPhieuInput, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True"/>
                        </StackPanel>

                        <StackPanel Margin="0,0,15,0">
                            <TextBlock Text="Tên Bệnh Nhân" Foreground="#94A3B8" FontSize="12" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding DSBenhNhan}" SelectedItem="{Binding BenhNhanDuocChon, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="TEN_BENHNHAN" SelectedValuePath="MA_BENHNHAN" IsEnabled="{Binding IsSua}" Height="28" HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Margin="5,0,0,0">
                            <TextBlock Text="Trạng Thái" Foreground="#94A3B8" FontSize="12" Margin="0,0,0,8"/>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Height="30">
                                <RadioButton Content="Chưa hoàn thành" 
                                             GroupName="TrangThaiGroup" 
                                             Foreground="White" 
                                             IsChecked="{Binding IsChuaHoanThanh, Mode=TwoWay}" 
                                             Margin="0,0,20,0"
                                             VerticalAlignment="Center"/>
                                <RadioButton Content="Đã hoàn thành" 
                                             GroupName="TrangThaiGroup" 
                                             Foreground="White" 
                                             IsChecked="{Binding IsDaHoanThanh, Mode=TwoWay}"
                                             VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </UniformGrid>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20" HorizontalAlignment="Center">
                <Button Content="{Binding TenNutThem}" Command="{Binding LenhThem}" Width="110" Height="35" Margin="0,0,10,0" Background="#10B981" Foreground="White" FontWeight="SemiBold"/>

                <Button Content="Chỉnh Sửa" Command="{Binding LenhSua}" IsEnabled="{Binding CoChon}" Width="110" Height="35" Margin="0,0,10,0" Style="{StaticResource ActionButtonStyle}"/>

                <Button Content="Xóa Hồ Sơ" Command="{Binding LenhXoa}" IsEnabled="{Binding CoChon}" Width="110" Height="35" Margin="0,0,10,0" Style="{StaticResource DangerButtonStyle}"/>

                <Button Content="Lưu Dữ Liệu" Command="{Binding LenhLuu}" IsEnabled="{Binding IsSua}" Width="130" Height="35" Margin="0,0,10,0" Style="{StaticResource SaveButtonStyle}"/>
            </StackPanel>

            <Border Grid.Row="2" Background="#1E293B" CornerRadius="12" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <DataGrid Grid.Row="1" 
                              Style="{StaticResource DarkDataGrid}" 
                              AutoGenerateColumns="False" 
                              IsReadOnly="True"
                              ItemsSource="{Binding DSPhieuKham}" 
                              SelectedItem="{Binding SelectedPhieuKham}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã Phiếu" Binding="{Binding MA_PHIEUKHAM}" Width="130"/>
                            <DataGridTextColumn Header="Mã Bệnh Nhân" Binding="{Binding MA_BENHNHAN}" Width="110"/>
                            <DataGridTextColumn Header="Mã NV Kê" Binding="{Binding MANV}" Width="140"/>
                            <DataGridTextColumn Header="Ngày Khám" Binding="{Binding NGAYKHAM, StringFormat='dd/MM/yyyy HH:mm'}" Width="*"/>
                            <DataGridTextColumn Header="Trạng Thái" Binding="{Binding TRANGTHAI}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

        </Grid>
    </Grid>
</UserControl>
