<UserControl x:Class="DoAn_QuanLyBenhVien.Views.UC_DanhSachBN"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DoAn_QuanLyBenhVien.Views"
             xmlns:glob="clr-namespace:System.Globalization;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="DarkRadioButton" TargetType="RadioButton">
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,20,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <BulletDecorator Background="Transparent" Cursor="Hand">
                            <BulletDecorator.Bullet>
                                <Grid Height="16" Width="16">
                                    <Ellipse x:Name="Border" 
                                         Stroke="#475569" 
                                         StrokeThickness="1" 
                                         Fill="#334155"/>
                                    <Ellipse x:Name="CheckMark" 
                                         Margin="4" 
                                         Fill="#10B981" 
                                         Visibility="Collapsed"/>
                                </Grid>
                            </BulletDecorator.Bullet>
                            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </BulletDecorator>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="Border" Property="Stroke" Value="#10B981"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Stroke" Value="#94A3B8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#475569"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#10B981"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                                <Setter Property="Background" Value="#1E293B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#475569"/>
            <Setter Property="Padding" Value="8,5"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DarkDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="#1E293B"/>
            <Setter Property="AlternatingRowBackground" Value="#1E293B"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#334155"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#334155"/>
                        <Setter Property="Foreground" Value="#94A3B8"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#475569"/>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.3"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#7F1D1D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.3"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#B91C1C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SaveButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.3"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="WarningButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F59E0B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.3"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="#0F172A">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#1E293B" CornerRadius="12" Padding="25" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,15,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Mã Bệnh Nhân " Foreground="#94A3B8" FontSize="13"/>
                            <TextBlock Text="*" Foreground="#EF4444" FontSize="13"/>
                        </StackPanel>
                        <TextBox Text="{Binding BNDangChon.MaBenhNhan, UpdateSourceTrigger=PropertyChanged}" 
                                 IsEnabled="{Binding IsSua}" 
                                 Style="{StaticResource DarkTextBox}" Margin="0,5,0,15"/>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Họ Và Tên Bệnh Nhân " Foreground="#94A3B8" FontSize="13"/>
                            <TextBlock Text="*" Foreground="#EF4444" FontSize="13"/>
                        </StackPanel>
                        <TextBox Text="{Binding BNDangChon.TenBenhNhan, UpdateSourceTrigger=PropertyChanged}" 
                                 IsEnabled="{Binding IsSua}" 
                                 Style="{StaticResource DarkTextBox}" Margin="0,5,0,15"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Ngày Sinh (dd/mm/yyyy) " Foreground="#94A3B8" FontSize="13"/>
                            <TextBlock Text="*" Foreground="#EF4444" FontSize="13"/>
                        </StackPanel>
                        <TextBox Text="{Binding BNDangChon.NgaySinh, UpdateSourceTrigger=LostFocus, 
                                StringFormat='{}{0:d}', ConverterCulture={x:Static glob:CultureInfo.CurrentCulture},
                                TargetNullValue=''}" IsEnabled="{Binding IsSua}" Style="{StaticResource DarkTextBox}" 
                                Margin="0,5,0,15"  Height="30"/>
                        <TextBlock Text="Giới Tính" Foreground="#94A3B8" FontSize="13" Margin="0,0,0,10" />
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15" IsEnabled="{Binding IsSua}">
                            <RadioButton Content="Nam" Style="{StaticResource DarkRadioButton}" IsChecked="{Binding IsNam}"/>
                            <RadioButton Content="Nữ" Style="{StaticResource DarkRadioButton}" IsChecked="{Binding IsNu}"/>
                            <RadioButton Content="Khác" Style="{StaticResource DarkRadioButton}" IsChecked="{Binding IsKhac}"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="15,0,0,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Số Điện Thoại " Foreground="#94A3B8" FontSize="13"/>
                            <TextBlock Text="*" Foreground="#EF4444" FontSize="13"/>
                        </StackPanel>
                        <TextBox Text="{Binding BNDangChon.SoDienThoai, UpdateSourceTrigger=PropertyChanged}" 
                                 IsEnabled="{Binding IsSua}" 
                                 Style="{StaticResource DarkTextBox}" Margin="0,5,0,15"/>

                        <TextBlock Text="Địa Chỉ Thường Trú" Foreground="#94A3B8" FontSize="13"/>
                        <TextBox Text="{Binding BNDangChon.DiaChi}" 
                                 IsEnabled="{Binding IsSua}" 
                                 Style="{StaticResource DarkTextBox}" Margin="0,5,0,15"/>

                        <TextBlock Text="Tiền Sử Bệnh Lý" Foreground="#94A3B8" FontSize="13"/>
                        <TextBox Text="{Binding BNDangChon.TienSuBenhLy}" 
                                 IsEnabled="{Binding IsSua}" 
                                 Style="{StaticResource DarkTextBox}" Margin="0,5,0,0" Height="35"/>
                    </StackPanel>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20" HorizontalAlignment="Center">
                <Button Content="{Binding TenNutThem}" 
                        Command="{Binding LenhThem}" 
                        Width="110" Height="38" Margin="0,0,10,0" 
                        Background="#10B981" Foreground="White" FontWeight="SemiBold"/>

                <Button Content="Chỉnh Sửa" 
                        Command="{Binding LenhSua}" 
                        IsEnabled="{Binding CoChon}" 
                        Width="110" Height="38" Margin="0,0,10,0" 
                        Style="{StaticResource ActionButtonStyle}"/>

                <Button Content="Xóa Hồ Sơ" 
                        Command="{Binding LenhXoa}" 
                        IsEnabled="{Binding CoChon}" 
                        Width="110" Height="38" Margin="0,0,10,0" 
                        Style="{StaticResource DangerButtonStyle}"/>

                <Button Content="Lưu Dữ Liệu" Command="{Binding LenhLuu}" IsEnabled="{Binding IsSua}" 
                        Width="130" Height="38" Margin="0,0,10,0" Style="{StaticResource SaveButtonStyle}"/>
                <Button Content="Thêm Phiếu Khám" 
                        Command="{Binding LenhThemPK}" IsEnabled="{Binding CoChon}" 
                        Width="160" Height="38" Style="{StaticResource WarningButtonStyle}"/>
            </StackPanel>

            <Border Grid.Row="2" Background="#1E293B" CornerRadius="12" Padding="10">
                <DataGrid ItemsSource="{Binding DSBN}" 
                          SelectedItem="{Binding BNDuocChon}" 
                          AutoGenerateColumns="False" 
                          IsReadOnly="True" 
                          Style="{StaticResource DarkDataGrid}"
                          SelectionMode="Single"
                          Height="300" 
                          ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã Bệnh Nhân" Binding="{Binding MaBenhNhan}" Width="110"/>
                        <DataGridTextColumn Header="Họ Và Tên Bệnh Nhân" Binding="{Binding TenBenhNhan}" Width="*"/>
                        <DataGridTextColumn Header="Ngày Sinh" Binding="{Binding NgaySinh, StringFormat={}{0:dd/MM/yyyy}}" Width="100"/>
                        <DataGridTextColumn Header="Giới Tính" Binding="{Binding GioiTinh}" Width="100"/>
                        <DataGridTextColumn Header="Số Điện Thoại" Binding="{Binding SoDienThoai}" Width="120"/>
                        <DataGridTextColumn Header="Ngày Lập Hồ Sơ" Binding="{Binding NgayTaoHoSo, StringFormat={}{0:dd/MM/yyyy}}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
