<UserControl x:Class="DoAn_QuanLyBenhVien.Views.UC_DichVu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DoAn_QuanLyBenhVien.Views"
             xmlns:vm="clr-namespace:DoAn_QuanLyBenhVien.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1100"
             Background="#0F1923">
    <UserControl.DataContext>
        <vm:DichVuViewModel/>
    </UserControl.DataContext>
    
    <UserControl.Resources>
        <Style TargetType="Control" x:Key="FadeDisabledStyle">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="#2D4A6E" BorderThickness="1" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#00C2A8"/>
                            </Trigger>

                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#00C2A8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter TargetName="border" Property="Background" Value="#F0F0F0"/>
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#162431"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="RowBackground" Value="#1A2E45"/>
            <Setter Property="AlternatingRowBackground" Value="#1E344E"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="BorderBrush" Value="#253D5C"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#253D5C"/>
            <Setter Property="Foreground" Value="#00C2A8"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,8"/>
        </Style>
        <Style TargetType="Button">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.4"/>
                    <Setter Property="Background" Value="Gray"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#162431" CornerRadius="8" Padding="15" Margin="0,0,0,15" BorderBrush="#253D5C" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.8*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0" Margin="0,0,10,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,15,10">
                        <TextBlock Text="Mã Dịch Vụ" Foreground="#00C2A8" Margin="0,0,0,5" FontSize="11"/>
                        <TextBox Height="32" Text="{Binding MaDichVu}" IsEnabled="{Binding IsEditable}" HorizontalAlignment="Stretch"/>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,5,10">
                        <TextBlock Text="Tên Dịch Vụ" Foreground="#00C2A8" Margin="0,0,0,5" FontSize="11"/>
                        <TextBox Height="32" Text="{Binding TenDichVu}" IsEnabled="{Binding IsEditable}" HorizontalAlignment="Stretch"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,15,5">
                        <TextBlock Text="Giá Dịch Vụ" Foreground="#00C2A8" Margin="0,0,0,5" FontSize="11"/>
                        <TextBox Height="32" Text="{Binding GiaDV, StringFormat='N0'}" IsEnabled="{Binding IsEditable}" HorizontalAlignment="Stretch"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1" VerticalAlignment="Bottom" Margin="0,0,5,12">
                        <CheckBox Content="Đang áp dụng dịch vụ" Foreground="White" IsChecked="{Binding DangApDung}" IsEnabled="{Binding IsEditable}"/>
                    </StackPanel>
                </Grid>

                <DockPanel Grid.Column="1" Margin="10,0,0,5">
                    <TextBlock Text="Mô Tả Chi Tiết" Foreground="#00C2A8" Margin="0,0,0,5" FontSize="11" DockPanel.Dock="Top"/>
                    <TextBox Text="{Binding MoTa}" IsEnabled="{Binding IsEditable}"
                     TextWrapping="Wrap" 
                     AcceptsReturn="True" 
                     VerticalContentAlignment="Top"
                     VerticalScrollBarVisibility="Auto"
                     Padding="8"
                     MinHeight="85"/>
                </DockPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15" HorizontalAlignment="Center">
            <Button Content="{Binding AddButtonContent}" Command="{Binding AddCommand}" Width="90" Margin="0,0,10,0" Background="#00C2A8" Foreground="White"/>
            <Button Content="📝 Sửa" Command="{Binding UpdateCommand}" IsEnabled="{Binding IsEditDeleteEnabled}" Width="90" Margin="0,0,10,0" Background="#FFB900" Foreground="White"/>
            <Button Content="🗑 Xóa" Command="{Binding DeleteCommand}" IsEnabled="{Binding IsEditDeleteEnabled}" Width="90" Margin="0,0,10,0" Background="#FF4655" Foreground="White"/>
            <Button Content="💾 Lưu" Command="{Binding SaveCommand}" IsEnabled="{Binding IsSaveEnabled}" Width="90" Background="#2196F3" Foreground="White"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding DanhSachDichVu}" SelectedItem="{Binding SelectedDichVu}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã Dịch Vụ" Binding="{Binding MA_DICHVU}" Width="120"/>
                <DataGridTextColumn Header="Tên Dịch Vụ" Binding="{Binding TEN_DICHVU}" Width="2*"/>
                <DataGridTextColumn Header="Giá DV" Binding="{Binding GIA_DV, StringFormat=N0}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
