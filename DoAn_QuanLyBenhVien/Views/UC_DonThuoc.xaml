<UserControl x:Class="DoAn_QuanLyBenhVien.Views.UC_DonThuoc"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DoAn_QuanLyBenhVien.Views"
             xmlns:vm="clr-namespace:DoAn_QuanLyBenhVien.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1100"
             Background="#0F1923">

    <UserControl.DataContext>
        <vm:VM_DonThuoc/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style x:Key="DarkTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#475569"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" 
                        CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#10B981"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                                <Setter Property="Background" Value="#1E293B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Control" x:Key="FadeDisabledStyle">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#2D4A6E"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid x:Name="Root">
                            <Border x:Name="Underline" 
                                BorderThickness="0,0,0,3" 
                                BorderBrush="Transparent" 
                                Margin="5,0"
                                VerticalAlignment="Bottom"/>
                            <ContentPresenter x:Name="ContentSite"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        ContentSource="Header"
                                        Margin="12,10"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="True">
                                <Setter TargetName="ContentSite" Property="TextBlock.Foreground" Value="#00C2A8"/>
                            </Trigger>

                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Underline" Property="BorderBrush" Value="#00C2A8"/>
                                <Setter TargetName="ContentSite" Property="TextBlock.FontWeight" Value="Bold"/>
                                <Setter TargetName="ContentSite" Property="TextBlock.Foreground" Value="#00C2A8"/>
                            </Trigger>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ContentSite" Property="TextBlock.Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#162431"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#253D5C"/>
            <Setter Property="RowBackground" Value="#1A2E45"/>
            <Setter Property="AlternatingRowBackground" Value="#1E344E"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#253D5C"/>
            <Setter Property="Foreground" Value="#00C2A8"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#00C2A8"/>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.3"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#7F1D1D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.3"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#B91C1C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SaveButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.3"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="WarningButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F59E0B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.3"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <TabControl Background="Transparent" BorderThickness="0" SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <TabItem Header="ĐƠN THUỐC">
                <Grid Margin="0,15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Background="#162431" CornerRadius="8" Padding="15" Margin="0,0,0,15" BorderBrush="#253D5C" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,20,10" Grid.Column="0">
                                <TextBlock Text="Mã Đơn Thuốc" Foreground="#00C2A8" Margin="0,0,0,5" FontSize="11"/>
                                <TextBox Height="32" Text="{Binding MaDonThuocMoi}" IsReadOnly="True" Style="{StaticResource DarkTextBox}"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,20,10" Grid.Column="1">
                                <TextBlock Text="Mã Phiếu Khám" Foreground="#00C2A8" Margin="0,0,0,5" FontSize="11"/>
                                <ComboBox ItemsSource="{Binding DanhSachPhieuKhamCbo}" SelectedItem="{Binding SelectedPhieuKhamCbo}" IsEnabled="{Binding IsProcessing}" DisplayMemberPath="MA_PHIEUKHAM" SelectedValuePath="MA_PHIEUKHAM" Height="30"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Grid Grid.Row="1" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                            <Button Content="{Binding AddButtonContent}" Command="{Binding AddCommand}" Width="110" Height="35" Margin="0,0,10,0" Background="#10B981" Foreground="White" FontWeight="SemiBold"/>
                            <Button Content="📝 Sửa" Command="{Binding UpdateCommand}" IsEnabled="{Binding EditDeleteVisibility}" Width="110" Height="35" Margin="0,0,10,0" Style="{StaticResource ActionButtonStyle}"/>
                            <Button Content="🗑 Xóa" Command="{Binding DeleteCommand}" IsEnabled="{Binding EditDeleteVisibility}" Width="110" Height="35" Margin="0,0,10,0" Style="{StaticResource DangerButtonStyle}"/>
                            <Button Content="💾 Lưu" Command="{Binding SaveCommand}" IsEnabled="{Binding SaveButtonVisibility}" Width="130" Height="35" Margin="0,0,10,0" Style="{StaticResource SaveButtonStyle}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="🔍 Tìm kiếm:" Foreground="#94A3B8" FontSize="13" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                        </StackPanel>
                    </Grid>

                    <DataGrid Grid.Row="2" ItemsSource="{Binding DanhSachDonThuoc}" SelectedItem="{Binding SelectedDonThuoc}" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã Đơn" Binding="{Binding MaDonThuoc}" Width="*"/>
                            <DataGridTextColumn Header="Mã Phiếu" Binding="{Binding MaPhieuKham}" Width="*"/>
                            <DataGridTextColumn Header="Ngày Kê" Binding="{Binding NgayKe, StringFormat='dd/MM/yyyy'}" Width="*"/>
                            <DataGridTextColumn Header="Mã NV Kê" Binding="{Binding MaNV_Ke}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="CHI TIẾT ĐƠN THUỐC">

                <Grid Margin="0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35*"/>
                        <ColumnDefinition Width="1021*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Background="#162431" CornerRadius="8" Padding="15" Margin="0,0,0,200" BorderBrush="#253D5C" BorderThickness="1" Grid.ColumnSpan="2">
                        <WrapPanel>
                            <StackPanel Margin="0,0,20,10">
                                <TextBlock Text="Mã Đơn Thuốc" Foreground="#00C2A8" Margin="0,0,0,5" FontSize="11"/>
                                <TextBox Width="200" Height="32" Text="{Binding SelectedDonThuoc.MaDonThuoc}" IsReadOnly="True" Style="{StaticResource DarkTextBox}"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,20,10">
                                <TextBlock Text="Chọn Thuốc" Foreground="#00C2A8" Margin="0,0,0,5" FontSize="11"/>
                                <ComboBox Width="250" Height="32" ItemsSource="{Binding DanhSachThuocGoc}" IsEnabled="{Binding IsProcessing}"
                                        SelectedItem="{Binding SelectedThuocHienTai}" DisplayMemberPath="TenThuoc" />
                            </StackPanel>

                            <StackPanel Margin="0,0,20,10">
                                <TextBlock Text="Số Lượng" Foreground="#00C2A8" Margin="0,0,0,5" FontSize="11"/>
                                <TextBox Width="70" Height="32" Text="{Binding SoLuongMoi, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsProcessing}" Style="{StaticResource DarkTextBox}"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,20,10">
                                <TextBlock Text="Thành Tiền" Foreground="#00C2A8" Margin="0,0,0,5" FontSize="11"/>
                                <TextBox Width="90" Height="32" Text="{Binding ThanhTienMoi, StringFormat='N0'}" IsReadOnly="True" Style="{StaticResource DarkTextBox}"/>
                            </StackPanel>
                        </WrapPanel>
                    </Border>

                    <StackPanel Orientation="Horizontal" Margin="243,109,0,150" HorizontalAlignment="Left" Grid.Column="1">
                        <Button Content="{Binding AddButtonContent}" Command="{Binding AddCommand}" Width="110" Height="35" Margin="0,0,10,0" Background="#10B981" Foreground="White" FontWeight="SemiBold"/>

                        <Button Content="📝 Sửa" Command="{Binding UpdateCommand}" IsEnabled="{Binding EditDeleteVisibility}" Width="110" Height="35" Margin="0,0,10,0" Style="{StaticResource ActionButtonStyle}"/>

                        <Button Content="🗑 Xóa" Command="{Binding DeleteCommand}" IsEnabled="{Binding EditDeleteVisibility}" Width="110" Height="35" Margin="0,0,10,0" Style="{StaticResource DangerButtonStyle}"/>

                        <Button Content="💾 Lưu" Command="{Binding SaveCommand}" IsEnabled="{Binding SaveButtonVisibility}" Width="130" Height="35" Margin="0,0,10,0" Style="{StaticResource SaveButtonStyle}"/>
                    </StackPanel>

                    <DataGrid ItemsSource="{Binding DanhSachChiTietDon_HienThi}" SelectedItem="{Binding SelectedChiTiet}" AutoGenerateColumns="False" IsReadOnly="True" Grid.ColumnSpan="2" Margin="0,164,0,0" Grid.RowSpan="3">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã Đơn Thuốc" Binding="{Binding MaDonThuoc}" Width="100"/>
                            <DataGridTextColumn Header="Mã Thuốc" Binding="{Binding MaThuoc}" Width="100"/>
                            <DataGridTextColumn Header="Tên Thuốc" Binding="{Binding TenThuoc}" Width="2*"/>
                            <DataGridTextColumn Header="Số Lượng" Binding="{Binding SoLuong}" Width="80"/>
                            <DataGridTextColumn Header="Thành Tiền" Binding="{Binding ThanhTien, StringFormat={}{0:N0} VNĐ}" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>

